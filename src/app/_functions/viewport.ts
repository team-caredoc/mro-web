import type { Viewport } from "next";

// TODO Viewport
export const viewport: Viewport = {
  initialScale: 1,
  userScalable: false,
  maximumScale: 1.0,
  minimumScale: 1,
  width: "device-width",
  themeColor: "F7F6F5",
};
